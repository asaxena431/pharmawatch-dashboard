# Dependencies for the standalone extraction scripts
# (extract_dictionary.py, extract_medspacy.py). Kept separate from the Flask
# app's requirements.txt so the web deploy stays lightweight.
#
# After installing, download the spaCy English model:
#   python -m spacy download en_core_web_sm
medspacy==1.3.1
spacy==3.8.14
flashtext==2.7
